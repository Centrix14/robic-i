Редактор получает запрос на выбор по (x,y).

Редактор преобразует (x,y) в Point. Он поочерёдно отправляет менеджерам сообщение getSelectionList(spatia, cursor).

Менеджеры получают сообщение getSelectionList(spatia, cursor) и отправляют на выход массив пар [key, object]. key — это значение тега id, который соответствует данному object.

Редактор берёт key и по нему получает список SVGElement. Редактор поочерёдно отправляет менеджерам сообщение select(object, SVGElement).

Менеджеры добавляют object в список выделенного и далее отправляют каждому object сообщение select(SVGElement).